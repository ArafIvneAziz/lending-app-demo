# Port to run the server on
PORT=4000

# MongoDB Connection String
MONGODB_CONNECTION_STRING=MONGODB_CONNECTION_STRING

# JWT Secret for encoding and decoding JWTs
JWT_SECRET=your-JWT_SECRET

# Stripe API Key
STRIPE_SECRET_KEY=your-STRIPE_SECRET_KEY

# Twilio API Key
TWILIO_API_KEY=your-twilio-api-key

# Twilio API Secret
TWILIO_API_SECRET=your-twilio-api-secret

# Twilio Phone Number
TWILIO_PHONE_NUMBER=your-twilio-phone-number

#Please replace your-jwt-secret, your-stripe-api-key, your-twilio-api-key, your-twilio-api-secret, and your-twilio-phone-number with your actual secret keys and values. Remember, never commit the .env file to version control. You should add .env to your .gitignore file to prevent it from being committed. Note: This is a mock .env file for demonstration purposes only. It doesn't include actual secret keys and values. Always keep your secrets secure and never share them publicly.